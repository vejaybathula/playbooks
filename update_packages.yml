---
- hosts: all
  tasks:
    - name: upgrade all packages 
      yum:
         name: '*'
         state: latest
         
    - name: install the latest version of Apache
      yum:
        name: httpd
        state: latest
   
